# Core LLM Fallback Chain
OLLAMA_BASE_URL=http://localhost:11434
OPENAI_API_KEY=XXXXXXX
ANTHROPIC_API_KEY=XXXXXXX

# Langfuse Observability Tracing
LANGFUSE_PUBLIC_KEY="XXXXXXX"
LANGFUSE_SECRET_KEY="XXXXXXX"
LANGFUSE_HOST=http://localhost:3000

# Postgres for local DB storage if required outside Docker
POSTGRES_USER=postgres
POSTGRES_PASSWORD=XXXXXXX
POSTGRES_DB=bodmas_engine
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Disable default Traceloop auto-telemetry (to prevent 404 span batch errors)
TRACELOOP_TRACING_ENABLED=false
